Calibration images are loaded from **human portrait URLs** in `src/constants/calibration-faces.ts` (RandomUser stock headshots).

To use **local** files instead, add JPGs under this folder and set `CALIBRATION_FACE_URLS` to paths like `/calibration-faces/face-01.jpg`.
